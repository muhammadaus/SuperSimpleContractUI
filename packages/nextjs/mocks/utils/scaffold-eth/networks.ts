// Mock implementation of scaffold-eth network utilities
import { mainnet } from 'viem/chains';

export const setTargetNetwork = (network: any) => {
  console.log('Mock setTargetNetwork called with:', network);
  return;
}; 