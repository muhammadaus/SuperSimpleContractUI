{
  "installCommand": "yarn install",
  "buildCommand": "cd ../.. && yarn next:build",
  "outputDirectory": "packages/nextjs/.next",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
